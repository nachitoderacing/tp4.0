let numeros = []
for(let i = 0; i < 5; i++) {
    numeros.push=prompt("Ingrese un número:");
    numeros.push(numeros);
}

console.log(numeros);

    for (let index = 0; index < numeros.lenght; index++) 
    if (index = 20)
    index = index*2